정방행렬(Square Matrix, m=n)
diagonal, 주대각성분
대각함(trace)
Identity(항등행렬) -> identity to multiplication

전치행렬(transpose matrix) row <-> column 
$$Transpose(A)=A^T$$
Symmetric Matrix(대칭행렬) $A=A^T$
Skewed-symmetric matrix $A^T=-A,  diag(A)=O(zero)$
Triangular matrix(삼각행렬)

### 기본 행연산
1. 어떤 2개의 행을 서로 바꾼다 $R_i \leftrightarrow R_j$
2. 어떤행에 0이 아닌 상수를 곱한다 $cR_i \to R_i$
3. 어떤행에 상수를 곱한후 다른행에다 더한다 $R_j+cR_i \to R_j$

	REF
	RREF

